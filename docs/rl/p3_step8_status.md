# P3 Step 8 Status: Statistics and Stop/Go Gate

## Required outcome

Apply the frozen paired analysis to the untouched Step 7 results, decide every
predeclared utility check, disclose adverse metrics and retain an honest power
boundary. Produce defense-level state, reward and dwell diagnostics without
retraining or changing the selected controller.

## Decision vocabulary

`PASS_DESCRIPTIVE_INSUFFICIENT_POWER` means every utility check passes and P4
may use the frozen H4 controller, while statistical superiority remains
unclaimed because four pairs cannot produce an exact two-sided p-value below
0.05.

## Next gate

Step 9 may export the immutable policy/Q-table/profile ROM artifacts, model
card, checksums and P3 exit evidence. No policy revision is permitted.
